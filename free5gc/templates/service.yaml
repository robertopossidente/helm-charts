{{- if eq .Values.name "webapp" -}}
apiVersion: v1
kind: Service
metadata:
  name: webapp
spec:
  selector:
    app: webapp
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 32603
      protocol: TCP
  type: NodePort
{{- end }}
